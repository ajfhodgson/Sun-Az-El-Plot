Here is a html file that plots the solar az/el for times throughout the year. There are a few changes I'd like made, please.
At each step, ONLY CHANGE THE MINIMUM number of lines of code - don't rewrite the whole thing so I can't easily check the changes.
1. I would like all the parameters that can be entered in input fields, including "Motto or Address" (suitably sluggified), to be 
pre-settable as url query parameters.
2. On first loading, the program should parse the url parameres and calculate and display a new plot, with missing parameters defaulted as today 
3. After the user enters and changed parameters (that might override those in the URL), and clicking "Generate", 
rather than calculating and displaying the chart, the program should instead create a new URL with the needed parameters and load 
the page which will then do the calculation and display of the chart
4. I would like the image that is placed bottom left to be a generated QR code that fully encodes the url and query parameters, so 
scanning the QR code generates the same plot
5. I would like the program to fetch a satellite view tile from Google Maps corresponding to the lat/long, and display this in the centre 
of the chart. This should be controlled by an additional parameter indicating the scale/zoom level of the satellite image, with 0 
meaning no satellite image to be fetched and displayed.
Before implementing these, please review C:\Users\andre\Repos\Sun-Az-El-Plot\Polar-Sun-Az-El-Plot.html, ask afor any clarifications required and 
comment on the feasibility of these changes, particularly 4 and 5.
